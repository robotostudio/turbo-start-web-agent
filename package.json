{
  "name": "turbo-start-web-agent",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "pnpm --filter @workspace/web run dev",
    "start": "pnpm --filter @workspace/web run start",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "lint": "biome check scripts harness.config.json && pnpm --filter @workspace/web run lint",
    "test": "node --experimental-strip-types --test \"scripts/**/*.test.ts\" && pnpm --filter @workspace/web run test",
    "content:check": "pnpm --filter @workspace/web run content:check",
    "catalog:check": "pnpm --filter @workspace/web run catalog:check",
    "gallery:check": "pnpm --filter @workspace/web run gallery:check",
    "harness": "node --experimental-strip-types scripts/harness-gen/index.ts",
    "harness:check": "node --experimental-strip-types scripts/harness-gen/index.ts --check",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "@types/node": "^24",
    "typescript": "~5.9.3"
  }
}
