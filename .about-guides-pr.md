## The change

Adds a closing call-to-action to the About page that directs visitors to the guides.

## Verification

- `pnpm content:check`
- `pnpm build`
- Browser preview verification was unavailable because the preview sandbox was not ready.
